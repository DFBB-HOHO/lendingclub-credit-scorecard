# Notebook area

This directory is intentionally reserved for interview walkthroughs and ad-hoc
exploration. The production-style source of truth is the tested pipeline under
`src/` and `scripts/train_scorecard.py`; results should not depend on hidden
notebook state.

